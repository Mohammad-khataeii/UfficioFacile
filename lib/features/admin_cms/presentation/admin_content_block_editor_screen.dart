import 'package:flutter/material.dart';

class AdminContentBlockEditorScreen extends StatelessWidget {
  const AdminContentBlockEditorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
